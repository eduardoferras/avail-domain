{
  "name": "avail-domain",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "changeset": "changeset",
    "version": "changeset version && npm install",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8"
  }
}
